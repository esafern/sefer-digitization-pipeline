# Drifted rulings - the ones the applier will not touch

39 ruling(s), generated by `tools/list_drifted_rulings.py`. **Regenerate after any apply** - the addresses move.

Each of these named a word by its position, and the position no longer names that word. The applier refuses rather than write to a place it cannot verify, and `repoint_stale_decisions.py` has already taken every case where the ink and the text agree - so everything below is a case where they disagree, only one of them exists, or neither does.

**The ink is the stronger signal.** It is the scan position recorded with the ruling, resolved through the same geometry the dashboard highlights with. Where a link is given it opens the word the ink points at, which is the one to look at - not the rotted index.

## The ink and the text point at DIFFERENT words (1)

Read the scan at the ink's word. If the ruling belongs there, re-rule at that index; if it belongs at the text's word, the bbox is stale and the text wins.

- [klal 66 · w17](http://127.0.0.1:8420/klal/66/word/34) — ruled on `None` → chose `ביטול`; that index now holds `ביטול`; ink says **w34**; text finds it at w17, w24

## Only the ink has an answer (15)

The word the ruling names is no longer anywhere in the klal - usually because a later correction changed it. The ink still knows where the ruling was made.

- [klal 2 · w632](http://127.0.0.1:8420/klal/2/word/617) — ruled on `None` → chose `None`; that index now holds `הרמב"ם`; ink says **w617**
- [klal 4 · w32](http://127.0.0.1:8420/klal/4/word/19) — ruled on `None` → chose ``; that index now holds `ולא`; ink says **w19**
- [klal 4 · w35](http://127.0.0.1:8420/klal/4/word/19) — ruled on `None` → chose ``; that index now holds `מקומו`; ink says **w19**
- [klal 35 · w30](http://127.0.0.1:8420/klal/35/word/64) — ruled on `None` → chose ``; that index now holds `משמע`; ink says **w64**
- [klal 57 · w16](http://127.0.0.1:8420/klal/57/word/28) — ruled on `None` → chose ``; that index now holds `דלא`; ink says **w28**
- [klal 59 · w24](http://127.0.0.1:8420/klal/59/word/23) — ruled on `None` → chose ``; that index now holds `רבי`; ink says **w23**
- [klal 84 · w55](http://127.0.0.1:8420/klal/84/word/54) — ruled on `None` → chose ``; that index now holds `(past the end of the klal)`; ink says **w54**
- [klal 88 · w1149](http://127.0.0.1:8420/klal/88/word/1148) — ruled on `None` → chose `None`; that index now holds `(past the end of the klal)`; ink says **w1148**
- [klal 90 · w4](http://127.0.0.1:8420/klal/90/word/4) — ruled on `None` → chose `None`; that index now holds `המקובל`; ink says **w4**
- [klal 100 · w1](http://127.0.0.1:8420/klal/100/word/1) — ruled on `None` → chose ``; that index now holds `בית`; ink says **w1**
- [klal 106 · w46](http://127.0.0.1:8420/klal/106/word/46) — ruled on `None` → chose ``; that index now holds `:`; ink says **w46**
- [klal 164 · w55](http://127.0.0.1:8420/klal/164/word/41) — ruled on `None` → chose `None`; that index now holds `(past the end of the klal)`; ink says **w41**
- [klal 169 · w1045](http://127.0.0.1:8420/klal/169/word/1044) — ruled on `None` → chose ``; that index now holds `פ"ד`; ink says **w1044**
- [klal 175 · w173](http://127.0.0.1:8420/klal/175/word/172) — ruled on `None` → chose ``; that index now holds `(past the end of the klal)`; ink says **w172**
- [klal 176 · w475](http://127.0.0.1:8420/klal/176/word/475) — ruled on `None` → chose ``; that index now holds `זה`; ink says **w475**

## Only the text has an answer (15)

No usable scan position was recorded. The word is findable, but a text match is not evidence of position (Lesson 5) - confirm against the page before re-ruling.

- [klal 1 · w95](http://127.0.0.1:8420/klal/1/word/85) — ruled on `לכן` → chose `לכו`; that index now holds `דוכתי`; text finds it at w85
- [klal 36 · w108](http://127.0.0.1:8420/klal/36/word/0) — ruled on `לו` → chose ``; that index now holds `(past the end of the klal)`; text finds it at w0
- [klal 63 · w41](http://127.0.0.1:8420/klal/63/word/40) — ruled on `סד` → chose ``; that index now holds `(past the end of the klal)`; text finds it at w40
- [klal 74 · w417](http://127.0.0.1:8420/klal/74/word/415) — ruled on `רבא` → chose ``; that index now holds `רב`; text finds it at w415, w434, w666
- [klal 146 · w44](http://127.0.0.1:8420/klal/146/word/43) — ruled on `וכאדם` → chose `ובאדם`; that index now holds `שייך`; text finds it at w43
- [klal 174 · w116](http://127.0.0.1:8420/klal/174/word/18) — ruled on `None` → chose `אלא`; that index now holds `לא`; text finds it at w18, w135
- [klal 206 · w2](http://127.0.0.1:8420/klal/206/word/24) — ruled on `None` → chose `אלו`; that index now holds `או`; text finds it at w24, w45, w76, w91
- [klal 210 · w66](http://127.0.0.1:8420/klal/210/word/65) — ruled on `כקמייתא` → chose `כקמייתא`; that index now holds `ולא`; text finds it at w65
- [klal 210 · w67](http://127.0.0.1:8420/klal/210/word/65) — ruled on `כקמייתא` → chose `כקמייתא`; that index now holds `כבתרייתא`; text finds it at w65
- [klal 210 · w68](http://127.0.0.1:8420/klal/210/word/65) — ruled on `כקמייתא` → chose `כקמייתא`; that index now holds `דמשום`; text finds it at w65
- [klal 210 · w132](http://127.0.0.1:8420/klal/210/word/108) — ruled on `כתרייתא` → chose `בתרייתא`; that index now holds `אם`; text finds it at w108, w130
- [klal 210 · w133](http://127.0.0.1:8420/klal/210/word/108) — ruled on `כתרייתא` → chose `בתרייתא`; that index now holds `לא`; text finds it at w108, w130
- [klal 210 · w139](http://127.0.0.1:8420/klal/210/word/138) — ruled on `נכתכו` → chose `נכתבו`; that index now holds `:`; text finds it at w138
- [klal 210 · w140](http://127.0.0.1:8420/klal/210/word/138) — ruled on `נכתכו` → chose `נכתבו`; that index now holds `(past the end of the klal)`; text finds it at w138
- [klal 210 · w141](http://127.0.0.1:8420/klal/210/word/138) — ruled on `נכתכו` → chose `נכתבו`; that index now holds `(past the end of the klal)`; text finds it at w138

## Neither signal has an answer (5)

Nothing locates these but reading the klal. Cheapest treated as a fresh review of that word rather than a recovery.

- [klal 39 · w251](http://127.0.0.1:8420/klal/39/word/251) — ruled on `דבכולהן` → chose ``; that index now holds `היכי`
- [klal 74 · w442](http://127.0.0.1:8420/klal/74/word/442) — ruled on `!` → chose ``; that index now holds `נקט`
- [klal 74 · w443](http://127.0.0.1:8420/klal/74/word/443) — ruled on `!` → chose ``; that index now holds `לה`
- [klal 209 · w16](http://127.0.0.1:8420/klal/209/word/16) — ruled on `מסספיע` → chose ``; that index now holds `לעונשין`
- [klal 209 · w17](http://127.0.0.1:8420/klal/209/word/17) — ruled on `השם` → chose ``; that index now holds `שהם`

## The address is fine - it is the CANDIDATE that went stale (3)

Both signals point at the recorded index, and repoint_stale_decisions.py agrees: it calls these `ok`, because the word is still there. The applier refuses for a different reason - the corrections entry the ruling hangs on no longer matches live text. Nothing needs re-pointing; the question is whether the ruling still says what you want at that word. Several are also cases close_satisfied_rulings.py declined because the chosen word repeats in the klal and no bbox corroborates which instance is meant - `אליבא` occurs 11 times in klal 91.

- [klal 91 · w453](http://127.0.0.1:8420/klal/91/word/453) — ruled on `איכא` → chose `אליבא`; that index now holds `אליבא`; ink says **w453**; text finds it at w7, w293, w338, w363
- [klal 91 · w524](http://127.0.0.1:8420/klal/91/word/524) — ruled on `איכא` → chose `אליבא`; that index now holds `אליבא`; ink says **w524**; text finds it at w7, w293, w338, w363
- [klal 210 · w131](http://127.0.0.1:8420/klal/210/word/131) — ruled on `הוא` → chose `הוא`; that index now holds `הוא`; ink says **w131**; text finds it at w109, w131

