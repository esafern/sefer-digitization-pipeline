# Drifted rulings - the ones the applier will not touch

> **Hebrew in this file is in VISUAL order** so it reads correctly in a viewer that runs no bidi algorithm. **Do not copy Hebrew out of it** - it will paste reversed. Regenerate with `--hebrew logical` for a copy-safe version.


15 ruling(s), generated by `tools/list_drifted_rulings.py`. **Regenerate after any apply** - the addresses move.

Each of these named a word by its position, and the position no longer names that word. The applier refuses rather than write to a place it cannot verify, and `repoint_stale_decisions.py` has already taken every case where the ink and the text agree - so everything below is a case where they disagree, only one of them exists, or neither does.

**The ink is the stronger signal.** It is the scan position recorded with the ruling, resolved through the same geometry the dashboard highlights with. Where a link is given it opens the word the ink points at, which is the one to look at - not the rotted index.

## The ink and the text point at DIFFERENT words (1)

Read the scan at the ink's word. If the ruling belongs there, re-rule at that index; if it belongs at the text's word, the bbox is stale and the text wins.

- [klal 106 · w46](http://127.0.0.1:8420/klal/106/word/26) — ruled on `:` -> chose ``; that index now holds `(past the end of the klal)`; ink says **w26**; text finds it at w33

## Only the ink has an answer (1)

The word the ruling names is no longer anywhere in the klal - usually because a later correction changed it. The ink still knows where the ruling was made.

- [klal 4 · w35](http://127.0.0.1:8420/klal/4/word/19) — insertion proposal - DocAI reads `1` here and the corpus does not have it; the reviewer REJECTED it; that index now holds `ומוקמ`; ink says **w19**

## Only the text has an answer (7)

No usable scan position was recorded. The word is findable, but a text match is not evidence of position (Lesson 5) - confirm against the page before re-ruling.

- [klal 36 · w108](http://127.0.0.1:8420/klal/36/word/0) — ruled on `ול` -> chose ``; that index now holds `(past the end of the klal)`; text finds it at w0
- [klal 74 · w417](http://127.0.0.1:8420/klal/74/word/415) — ruled on `אבר` -> chose ``; that index now holds `בר`; text finds it at w415, w434, w666
- [klal 159 · w10](http://127.0.0.1:8420/klal/159/word/15) — ruled on `None` -> chose `אבילא`; that index now holds `אכיא`; text finds it at w15, w87, w316, w361
- [klal 174 · w116](http://127.0.0.1:8420/klal/174/word/18) — ruled on `None` -> chose `אלא`; that index now holds `אל`; text finds it at w18, w135
- [klal 200 · w145](http://127.0.0.1:8420/klal/200/word/166) — ruled on `None` -> chose `ולא`; that index now holds `וא`; text finds it at w166, w236
- [klal 206 · w2](http://127.0.0.1:8420/klal/206/word/24) — ruled on `None` -> chose `ולא`; that index now holds `וא`; text finds it at w24, w45, w76, w91
- [klal 216 · w123](http://127.0.0.1:8420/klal/216/word/37) — ruled on `None` -> chose `אלא`; that index now holds `אל`; text finds it at w37, w88, w144, w375

## Neither signal has an answer (6)

Nothing locates these but reading the klal. Cheapest treated as a fresh review of that word rather than a recovery.

- [klal 39 · w251](http://127.0.0.1:8420/klal/39/word/251) — ruled on `ןהלוכבד` -> chose ``; that index now holds `יכיה`
- [klal 74 · w442](http://127.0.0.1:8420/klal/74/word/442) — ruled on `!` -> chose ``; that index now holds `טקנ`
- [klal 74 · w443](http://127.0.0.1:8420/klal/74/word/443) — ruled on `!` -> chose ``; that index now holds `הל`
- [klal 161 · w289](http://127.0.0.1:8420/klal/161/word/289) — ruled on `None` -> chose `לאנתנ`; that index now holds `ןנתנ`
- [klal 209 · w16](http://127.0.0.1:8420/klal/209/word/16) — ruled on `עיפססמ` -> chose ``; that index now holds `ןישנועל`
- [klal 209 · w17](http://127.0.0.1:8420/klal/209/word/17) — ruled on `םשה` -> chose ``; that index now holds `םהש`

